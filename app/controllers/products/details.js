import Controller from '@ember/controller';
// import { inject as service } from '@ember/service';

export default class ProductsDetailsController extends Controller {
//   @service productStore;

//   get product() {
//     return this.productStore.fetchProduct(this.model.id);
//   }
}
